# Tasks - Unified Block Visualization Panel (Progressive Disclosure)

- [x] 1. Create `BlockViewer.vue` component at `src/components/editor/BlockViewer.vue`
- [x] 2. Modify `TreeEditor.vue` to adopt the split-panel layout (35% tree, 65% content) and use `BlockViewer`
- [x] 3. Modify `TextEditor.vue` to support flat/list concepts and use `BlockViewer`
- [x] 4. Modify `App.vue` to display only the left sidebar at startup, keeping the center empty until selection
