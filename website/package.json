{
  "name": "format-website",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs && npx serve ../docs"
  },
  "dependencies": {
    "marked": "^12.0.0"
  }
}
